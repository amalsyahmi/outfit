import * as React from 'react';
import {View, Text} from 'react-native';

export const SplashScreen = () => {
  return (
    <View style={{width: '100%', height: '100%', backgroundColor: '#DB3022'}}>
      <Text style={{margin: 'auto', color: '#ffffff', fontSize: 100}}>
        Outfit
      </Text>
    </View>
  );
};
